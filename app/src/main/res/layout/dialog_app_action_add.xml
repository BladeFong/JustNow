<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/s_app_action_add_title"
        android:textAppearance="@style/TextAppearance.JustNow.Title"
        android:layout_marginBottom="12dp" />

    <AutoCompleteTextView
        android:id="@+id/et_app_search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/s_app_search_hint"
        android:inputType="text"
        android:maxLines="1"
        android:completionThreshold="1"
        android:textAppearance="@style/TextAppearance.JustNow.Body"
        android:padding="12dp"
        android:drawablePadding="16dp"
        android:dropDownHeight="200dp" />

    <EditText
        android:id="@+id/et_app_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/s_app_hint_hint"
        android:inputType="text"
        android:maxLines="1"
        android:textAppearance="@style/TextAppearance.JustNow.Caption"
        android:padding="12dp"
        android:layout_marginTop="8dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/btn_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Widget.JustNow.Dialog.Button.Secondary"
            android:text="@string/s_cancel" />

        <Button
            android:id="@+id/btn_add_to_list"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Widget.JustNow.Dialog.Button"
            android:text="@string/s_add"
            android:enabled="false" />
    </LinearLayout>
</LinearLayout>
