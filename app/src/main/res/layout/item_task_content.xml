<?xml version="1.0" encoding="utf-8"?>
<!-- 统一任务项内容布局，主界面 RecyclerView 和 Widget 共用 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_task_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="2dp">

    <!-- 左侧象限色条（RemoteViews 兼容：TextView + background，不是 View） -->
    <TextView
        android:id="@+id/v_quadrant_color"
        android:layout_width="6dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="8dp"
        android:background="@color/quadrant_not_urgent_important" />

    <!-- 内置任务图标 ImageView -->
    <ImageView
        android:id="@+id/iv_task_icon"
        android:layout_width="@dimen/task_icon_size"
        android:layout_height="@dimen/task_icon_size"
        android:layout_marginEnd="8dp"
        android:scaleType="fitCenter"
        android:visibility="gone" />

    <!-- 文字区域 -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- 第一行：专注时长 + 标签 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tv_focus_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="@style/TextAppearance.JustNow.Caption"
                android:textColor="@color/text_tertiary"
                android:singleLine="true" />

            <TextView
                android:id="@+id/tv_tag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:textAppearance="@style/TextAppearance.JustNow.Body"
                android:textColor="@color/tag_normal"
                android:singleLine="true"
                android:ellipsize="end"
                android:visibility="gone" />
        </LinearLayout>

        <!-- 第二行：任务标题 -->
        <TextView
            android:id="@+id/tv_task_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.JustNow.Body"
            android:textColor="@color/text_primary"
            android:singleLine="true"
            android:ellipsize="end" />
    </LinearLayout>
</LinearLayout>
