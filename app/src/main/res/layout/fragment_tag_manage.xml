<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background_surface">

    <TextView
        android:id="@+id/tv_priority_group_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="8dp"
        android:text="@string/s_period_group_priority_hint"
        android:textAppearance="@style/TextAppearance.JustNow.Caption"
        android:textColor="@color/text_hint" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_priority_groups"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:nestedScrollingEnabled="false"
        android:elevation="1dp" />

    <!-- 占位空间 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- 清理未使用标签 -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_cleanup_tags"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/s_unused_tags_title"
        android:textAppearance="@style/TextAppearance.JustNow.Body"
        android:layout_margin="16dp" />

</LinearLayout>
